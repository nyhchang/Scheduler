import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export default class App extends React.Component {
  render() {
    return (
        <View>
          /*Empty for now but look into react native elements docs and find out how to put in some dank login page stuff
           */
        </View>
    );
  }
}